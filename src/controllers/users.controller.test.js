import test from 'ava';
// import getUsers from './users.controller';

test('Should export controller functions', (t) => {
  t.is(true, true);
  // const controller = getUsers();
  // expect(typeof controller).toBe('object');
});
